node_modules/
*.log
.DS_Store

# secrets — never commit
.cerver-key

# generated views — regenerated by `nightly dashboard` / `nightly chat`.
# the embeddable widget (web/cerver-chat.js) IS tracked; the rendered pages are not.
web/index.html
web/report.html
